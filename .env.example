# mac-remote-mcp environment variables

# REQUIRED: Bearer token for API authentication
# Generate: openssl rand -hex 32
MCP_API_KEY=

# Server settings
TRANSPORT=http
PORT=3000
HOST=127.0.0.1

# Playwright MCP proxy
PLAYWRIGHT_PORT=3001
PROXY_PORT=3002
